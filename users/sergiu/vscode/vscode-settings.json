{
    "nix.enableLanguageServer": true,
    "nix.serverSettings": {
        "nixd": {
            "nixpkgs": {
                "expr": "import <nixpkgs> { overlays = [ (import /home/sergiu/NixOS/flake.nix).outputs.overlays.default or (_: _: {}) ]; }"
            }
        }
    },
    "git.enableSmartCommit": true
}